
import 'package:flutter/material.dart';

class AspectRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Aspect Page"),
      ),
    );
  }
}